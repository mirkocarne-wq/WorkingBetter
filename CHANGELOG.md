# Changelog

Tutte le modifiche rilevanti a questo progetto sono documentate in questo file.

Il formato segue [Keep a Changelog](https://keepachangelog.com/it/1.1.0/) e il progetto adotta il [Semantic Versioning](https://semver.org/lang/it/).

## [Unreleased]

### Added
- **Sprint 12 — Consolidamento** (in corso).
  - Sicurezza API: test di invarianti (ogni rotta pubblica o con permesso; ogni tabella multi-tenant con RLS e policy), rate limiting in memoria sugli endpoint pubblici con `429` e `Retry-After`, revoca delle sessioni (`sessions_revoked_at`, controllo nel guard con cache, `POST /auth/logout-all`, revoca automatica su cambio/reset password e disattivazione; il cambio password restituisce una nuova sessione), header di sicurezza su API e web, cookie `Secure` in https, "Esci da tutti i dispositivi" in Impostazioni. Migrazione 0020.
- **Sprint 11 — Sviluppo e carriera, fase 1** (`DEV`).
  - Libreria pura in `packages/shared/src/development`: 11 competenze in italiano (trasversali, di ruolo, leadership) con 4 livelli e descrittori comportamentali, 23 azioni di sviluppo suggerite per competenza, calcolo del gap per fonte (policy manager / media / 360°), suggerimenti dai gap, avanzamento del piano, fasce di performance e etichette del 9-box (3 unit test).
  - Dati: `competencies`, `job_profiles` (competenze attese e ruolo successivo), `competency_assessments` (self, manager, review, 360°), `development_plans`, `development_actions`, `talent_assessments`; `persons.job_profile_id`; RLS (migrazioni 0018/0019); fatti `dev_has_profile`, `dev_has_plan`, `dev_actions_open/overdue` e metriche "Persone con profilo di ruolo", "Persone con piano di sviluppo", "Azioni di sviluppo scadute".
  - API `/development/*`: framework (libreria predefinita, competenze, job profile, assegnazione), profilo con gap e confronto con il ruolo successivo, autovalutazione e valutazione del manager on demand, piani con azioni (da gap, review, 1:1, manuali), invio in approvazione con notifica al manager, approvazione con nota, completamento, 9-box (performance dall'ultima review condivisa/firmata, potenziale del manager con nota tracciata e storico, mai visibile alla persona), elenco persone del perimetro; permessi `development:use|team|manage`; 5 test e2e. I suggerimenti del 1:1 includono le azioni di sviluppo scadute o in scadenza; il worker notifica le azioni entro 3 giorni o scadute (1 test).
  - Web: **Sviluppo** (radar atteso vs valutato, tabella gap, "cosa manca per il ruolo successivo", autovalutazione, piano con azioni e approvazione, azioni suggerite da aggiungere con un clic, scheda team per i manager), pagina della persona per manager/HR con valutazione e talent review, **Sviluppo → Amministrazione** (9-box, persone e assegnazione profili, job profile, competenze con caricamento libreria). Seed: profili Engineering, valutazioni, piano attivo di Luca, potenziali.
- **Sprint 10 — Report builder sul semantic layer** (`ANA-050/051/052/054/055/060`).
  - Query engine estratto in `packages/db/src/analytics/query.ts` (`runMetricQuery`, `runTrend`, `latestSnapshot`) e riusato da API e worker; in `@wb/shared` la logica pura dei report: confronto con lo snapshot precedente (`compareRows`, variazione in punti per le percentuali), CSV con colonna variazione, calcolo del prossimo invio (`nextRunAt`, fuso del tenant); 3 unit test.
  - Tabella `saved_reports` (definizione dichiarativa, condivisione per ruolo/persona, pianificazione; RLS, migrazioni 0016/0017). API `/analytics/reports`: elenco (propri + condivisi), creazione con validazione nel perimetro di chi salva, modifica e cancellazione (solo proprietario), duplicazione, `run` con filtri dinamici (data, unità, manager, ciclo) e `format=csv`, invio immediato via email, anteprima destinatari. Chi apre un report condiviso vede **sempre e solo** i dati del proprio perimetro, con le soglie del catalogo.
  - Worker: job `reports` ogni 15 minuti — per ogni report con pianificazione scaduta invia il CSV a proprietario e, se richiesto, ai ruoli condivisi, calcolando il report **con il perimetro di ciascun destinatario** (HR: azienda, manager: team, altri: nessun invio); deduplica per giorno; 2 test.
  - Web: **Report → Report salvati** (cartelle, condivisione, prossimo invio), **Nuovo report** con costruzione guidata (metriche per modulo con dimensioni ammesse, dettaglio, filtri, confronto, tabella/barre/trend, condivisione per ruolo, invio automatico), pagina del report con filtri dinamici, variazione colorata accanto ai valori, grafico, CSV, "Inviami via email", duplica, modifica, elimina.
- **Sprint 9 — Calendario senza OAuth** (ADR-0010, INT-022/023/024/025, ONE-003 parziale).
  - Libreria iCalendar pura in `packages/shared/src/calendar`: costruzione RFC 5545/5546 (UID e SEQUENCE stabili, METHOD REQUEST/CANCEL, eventi tutto il giorno, escape e piegatura righe, VALARM), proposta di slot da orario di lavoro, giorni feriali e impegni noti, scostamento di fuso IANA (6 unit test).
  - **Inviti .ics via email** per ogni 1:1: creazione e prossimo incontro (REQUEST), riprogrammazione (REQUEST con SEQUENCE maggiore), annullamento (CANCEL); passano dalle preferenze del nuovo tipo di notifica `one_on_one.invite` (email attiva di default). La coda email supporta gli allegati e il worker li invia come `text/calendar; method=…` (nodemailer `icalEvent`).
  - **Feed iCalendar personale**: `GET /calendar/feed` (stato e anteprima), `POST /calendar/feed/rotate` (URL segreto, rigenerabile), `DELETE /calendar/feed`; il feed pubblico `GET /calendar/feed/:token.ics` contiene 1:1, scadenze self/manager review dei cicli attivi, chiusura delle survey a cui si è invitati e azioni in scadenza — solo titoli e date. `GET /meetings/:id.ics` per "Aggiungi al calendario".
  - `GET /one-on-ones/:id/slots`: tre proposte su giorni diversi, fuso del tenant, evitando gli altri 1:1 dei partecipanti; link videocall per relazione (`meetingUrl`) incluso in inviti e feed. Migrazione 0015. 5 test e2e, 2 test worker.
  - Web: **Impostazioni → Calendario** (attiva/rigenera/disattiva il feed, istruzioni per Google/Outlook/Apple, prossimi eventi); nella pagina del 1:1 riquadro "Quando" con slot proposti, riprogrammazione, durata, link videocall, annullamento, pulsanti "Entra nella videocall" e "Aggiungi al calendario".
- **Sprint 8 — Design system e client API generato** (ADR-0009).
  - Contratto OpenAPI completo: i decoratori `@ZBody`/`@ZQuery` pubblicano nel documento gli stessi schemi Zod usati per la validazione (182 operazioni con body e query tipizzati), `@ZOk` dichiara le risposte vincolanti (health, me, auth/config, auth/login, unread-count), schema `Problem` (RFC 9457) come risposta di errore di ogni operazione; `pnpm --filter @wb/api openapi:emit` genera `packages/api-client/openapi.json` avviando l'API su PGlite.
  - Nuovo pacchetto **`@wb/api-client`**: tipi generati con openapi-typescript, `createApiClient()` tipizzato su openapi-fetch per mobile e integrazioni, `request()` con percorso verificato a compile time, `ApiError`/`errorMessage` per i problem+json; 3 test. La web app passa da questo client: ogni chiamata è controllata contro il contratto (un refuso nel percorso è un errore di compilazione). CI: `pnpm contract:check` fallisce se contratto o tipi non sono aggiornati.
  - Design system: token CSS documentati (superfici, brand con derivate `color-mix`, stato, serie grafici, tipografia, spaziatura 4pt, forma, focus), primitive React in `components/ui.tsx` (PageHeader, Card, Kpi, Pill, Button, EmptyState, Field/Input/Select/Textarea/Checkbox, Tabs, VisibilityBadge, Avatar/Who, Progress, TableWrap), classe `.input` unica al posto di 100+ stili inline nei form, **guida di stile vivente** in `/settings/design`.
  - **Colore primario del tenant**: in Impostazioni → Aspetto l'amministratore imposta nome e colore; il layout lo applica come variabile `--brand` a pulsanti, tab, link attivi, barre e focus. `PATCH /tenant` ora fonde le impostazioni chiave per chiave (salvare l'aspetto non cancella la configurazione SSO).
  - Responsive e accessibilità: sidebar a cassetto sotto i 900 px (`AppShell`, chiusura con Esc e al cambio pagina), griglie a una colonna, tabelle e tab scorrevoli, link "Vai al contenuto", `aria-current`, focus visibile, `prefers-reduced-motion`, stile di stampa.
- **Sprint 7 — Welfare aziendale, fase 1** (`WEL`, ADR-0008): il primo modulo che non esiste in PeopleGoal.
  - Logica pura in `packages/shared/src/welfare`: preset delle soglie per anno (fringe benefit 1.000/2.000 € con figli a carico per il 2024–2027, 258,23 € altrimenti; previdenza complementare 5.164,57 €; sanità integrativa 3.615,20 €), saldo dal registro movimenti (accreditato, impegnato, speso, disponibile, in scadenza), cumulo per categoria e controllo soglia con quota imponibile, simulatore cash vs welfare del premio di risultato, regola di riporto (6 unit test).
  - Dati: `welfare_plans`, `welfare_budget_sources`, `welfare_movements` (registro **append-only**: accredito, prenotazione, rilascio, spesa, storno, scadenza, rettifica; il saldo è sempre calcolato), `welfare_categories`, `welfare_thresholds` per anno, `welfare_catalog_items`, `welfare_requests`, `welfare_declarations`, `welfare_initiatives` + adesioni, `welfare_payroll_batches` + voci; RLS su tutte; fatti `welfare_credited_year`, `welfare_spent_year`, `welfare_has_request_year`, `welfare_in_plan` e metriche "Take-up welfare" e "Budget welfare utilizzato" nel catalogo.
  - API `/welfare/*`: per il dipendente conto e movimenti, categorie con soglie e cumulo, catalogo, richieste di rimborso/voucher/servizio con prenotazione del budget e calcolo dell'eccedenza imponibile, annullamento, dichiarazioni annuali (figli a carico), iniziative con posti, simulazione e scelta irrevocabile della conversione del premio; per l'HR piani (bozza → attivo → chiuso), fonti di budget con accredito immediato o programmato, soglie da preset, categorie, catalogo, iniziative, coda di verifica (approva, chiedi documenti, rifiuta, con nota), rettifiche manuali con motivazione, anteprima e lotti payroll con CSV (rimborsi da liquidare, eccedenze da assoggettare, conversioni), conferma della liquidazione; notifiche `welfare.*`; 9 test e2e.
  - Worker: accredito automatico delle fonti alla data prevista e avvisi di budget in scadenza a 60/30/7 giorni (2 test).
  - Web: **Welfare** per il dipendente (saldo, soglie con barre di cumulo, nuova richiesta con controllo soglia e dichiarazione, catalogo con richiesta diretta, richieste, movimenti, iniziative, dichiarazioni, simulatore del premio) e **Welfare → Amministrazione** (coda di verifica, piani e fonti, catalogo e iniziative, categorie e soglie con preset, payroll con lotti e CSV). Seed: piano "Welfare 2026" con 800 € a persona, 8 categorie con soglie 2026, 5 voci di catalogo, richieste e iniziative di esempio.
- **Sprint 6 — Survey e pulse** (`ENG`), sul form engine con anonimato architetturale.
  - Libreria in `packages/shared/src/surveys`: 7 driver, 14 domande Likert validate in italiano, eNPS e commento, 4 template (engagement completa, pulse con rotazione delle domande, eNPS, benessere); funzioni pure per punteggi per driver e domanda, distribuzione, eNPS, heatmap con soglia e protezione per differenza, fasce di anzianità (12 unit test).
  - Dati: `surveys`, `survey_invitations` (solo flag "ha risposto") e `survey_responses` (risposte + segmento, senza persona nelle survey anonime), RLS; fatti `survey_invited_90d`/`survey_responded_90d` e metrica "Tasso di risposta survey (90 gg)" nel catalogo.
  - API `/surveys`: creazione da template (form pubblicato automaticamente) o da form esistente, anteprima popolazione, lancio con rifiuto sotto soglia e notifiche, compilazione una sola volta con validazione del form engine, solleciti ai soli non rispondenti (solo conteggi), proroga, chiusura, condivisione della sintesi, risultati (driver, domande, eNPS, commenti HR, heatmap per unità/manager/anzianità, confronto con la survey precedente), vista manager limitata al proprio team, sintesi pubblica per gli invitati; 6 test e2e.
  - Worker: chiusura automatica alla scadenza con avviso all'HR e promemoria a 3 e a 1 giorno dalla chiusura (2 test).
  - Web: **Survey** (le mie, gestione con creazione da template), compilazione con dichiarazione di anonimato, pagina risultati (KPI, eNPS, barre per driver con variazione, distribuzioni, heatmap, commenti, proroga, sollecito, chiusura, pubblicazione sintesi), pagina di sintesi per i rispondenti. Seed: una engagement condivisa con 8 risposte e una pulse aperta.
- **Sprint 5 — Autenticazione, inviti e SSO** (`CORE-014/030/031`, ADR-0007).
  - L'API emette le sessioni (JWT HS256, `AUTH_SESSION_SECRET`, TTL configurabile, `POST /auth/refresh`); `AUTH_MODE` = `dev` (login di sviluppo attivo) o `prod`.
  - Password locale: hash scrypt con formato autodescrittivo, policy minima, blocco 15 minuti dopo 5 tentativi, cambio password, reset via link monouso (1 ora) con email; `POST /auth/login`, `/auth/forgot-password`, `/auth/reset-password`, `PATCH /auth/password`.
  - Inviti: `POST /users/invite` (persona esistente o nuova, ruoli), email con link monouso (7 giorni), `GET /auth/invite/:token`, accettazione con password (o via SSO), reinvio, disattivazione/riattivazione utenti, `GET /users` con stato (invitato, attivo, disattivato, scaduto).
  - SSO OpenID Connect **per tenant**: configurazione in `Impostazioni → SSO` (issuer, client id, client secret cifrato con la chiave per tenant, provisioning automatico, ruolo di default, domini ammessi, password disabilitabile), flusso Authorization Code + PKCE gestito dall'API (`/auth/oidc/start`, `/auth/oidc/callback`, codice di scambio monouso, `/auth/exchange`), collegamento per subject o email, provisioning JIT; testato end-to-end con un identity provider finto in-process (11 test).
  - Web: pagina di accesso con password, pulsante SSO e accesso rapido di sviluppo separato; pagine invito, password dimenticata, reset, callback SSO; **Persone → Utenti e accessi** (inviti, ruoli, disattivazione); **Impostazioni** (SSO, cambio password). Seed: password demo `Password!2026` per tutti gli utenti. Docker: nuove variabili `AUTH_SESSION_SECRET`, `APP_BASE_URL`, `API_PUBLIC_URL`; guida Keycloak in `docs/12`.
- **Sprint 4b — creazione da web**: pagina **Nuovo obiettivo** (periodo, livello secondo i permessi, owner, unità, allineamento a un obiettivo padre, visibilità, key result dinamici, pubblicazione) e apertura di nuovi periodi per l'HR; **Nuovo questionario** con costruttore guidato (sezioni, domande tipizzate, scale con N/A, scelte con punteggio, obbligatorietà, pesi, pubblicazione immediata) e pubblicazione delle bozze dalla lista form; **Nuovo template di review** dalla scheda Cicli (questionari pubblicati, scadenze, regola di visibilità della self-review, scala e etichette del rating, campo di rating complessivo, firma, contesto obiettivi).
- **Sprint 4 — Fondamenta della reportistica** (`ANA`, ADR-0006).
  - Semantic layer v1 in `packages/shared/src/analytics`: catalogo dichiarativo di 26 metriche (persone, obiettivi, 1:1, feedback, review, form) con definizione leggibile, formula, calcolo (somma o rapporto di fatti), dimensioni ammesse, visibilità per il manager, flag sensibile e soglia minima di gruppo; validazione del catalogo all'avvio dell'API; motore di valutazione puro con soppressione sotto soglia e protezione per differenza (metriche sensibili).
  - Data mart v1: tabella `mart_person_facts` (fatti giornalieri a grana persona con manager e unità "alla data", RLS), `refreshMartForTenant` in `packages/db`, job del worker `mart-refresh` (giornaliero, `--once`), aggiornamento manuale dall'interfaccia; il seed genera 14 giorni di snapshot.
  - API `/analytics/*`: data dictionary, `query` (metriche × unità/manager/persona/ciclo, filtri, ultimo snapshot), `trend` giornaliero, `alerts` (senza obiettivi, senza 1:1, review scadute, KR senza check-in, azioni scadute, compilazioni in ritardo, senza manager), report di processo dei cicli di review (completamento per fase, unità e manager, ritardatari, tempi medi, distribuzione rating con soglia), export CSV con registrazione in audit (ANA-063); perimetro server-side: tutto per HR/analista, riporti diretti per il manager (nuovo permesso `analytics:query:team`).
  - Web: pagina **Report** (`/analytics`) con KPI dal catalogo, andamento con grafico interattivo, segnali, tabella per dimensione, report di processo (`/analytics/process/[cycleId]`), dizionario delle metriche, export CSV via `/api/export`; 7 test e2e API, 2 test worker, 6 unit test del motore.
- **Sprint 3 — Performance Review** (`REV`), costruito sul form engine.
  - Template di review (`REV-002/005/006/008/010`): form self e manager pubblicati, scadenze relative al lancio, regola di visibilità della self-review per il manager (subito / dopo l'invio / mai), scala di rating con etichette, rating complessivo da campo dedicato o derivato dal punteggio del form, snapshot del template nel ciclo.
  - Cicli (`REV-020/021/022/055/060/061`): popolazione per unità (con sottoalbero) e persone, esclusioni, anteprima con persone saltate perché senza manager; lancio che crea review e compilazioni self/manager e notifica persone e manager; avanzamento per stato e per manager; solleciti manuali (una volta al giorno) e promemoria automatici del worker a ridosso delle scadenze; chiusura del ciclo con annullamento delle review incomplete.
  - Review (`REV-031/034/051/052/053`): pannello di contesto con obiettivi del periodo, feedback visibili, riconoscimenti, 1:1 conclusi e review precedenti; condivisione con il collaboratore (con snapshot degli obiettivi), registrazione del colloquio, presa visione con commento e dissenso, override del rating da parte di HR con motivazione, riapertura di una fase; visibilità delle risposte calcolata lato server per soggetto, manager e HR.
  - Notifiche `review.launched`, `review.stage_due`, `review.shared`, `review.signed`; hook `onSubmitted` del form engine per collegare l'invio di una compilazione al suo contesto.
  - Web: `/reviews` (le mie, il mio team, cicli), `/reviews/cycles/[id]` (anteprima popolazione, lancio, solleciti, chiusura, avanzamento), `/reviews/[id]` (FormRunner per la propria fase, lettura delle fasi inviate, contesto, condivisione e firma). Seed con un ciclo attivo per il team Prodotto; 7 test e2e.
  - API: se l'host non supporta IPv6 il server ripiega automaticamente su IPv4.
- **Ambiente di test in Docker**: `Dockerfile` multi-stage (api, workers, web standalone), `docker-compose.yml` con profilo `app` (migrazioni + seed automatici, API, worker, web) e Mailpit per le email di prova, `Makefile` con comandi rapidi, guida passo-passo per macOS in `docs/12-ambiente-test-docker.md`. La web app usa `API_INTERNAL_URL` per le chiamate lato server dentro la rete Docker.
- **Sprint 2 — notifiche, import CSV, form engine.**
  - Notifiche (`INT-001…003`): centro notifiche in-app con conteggio non lette, preferenze per tipo e canale con default sensati, coda email (`email_outbox`) e helper `notify` condiviso; eventi collegati: feedback ricevuto, richiesta di feedback, riconoscimento, nuovo 1:1, azione assegnata, obiettivo off track (al manager, una volta al giorno), form assegnato, import completato.
  - Worker (`apps/workers`): job `reminders` (check-in in ritardo per cadenza del ciclo, 1:1 nelle prossime 24 h, azioni scadute, richieste di feedback in sospeso) idempotente per giorno tramite `dedupeKey`; job `email-dispatch` con 5 tentativi e backoff; scheduler BullMQ su Redis oppure in-process senza Redis; modalità `--once` per cron esterni e CI; tabella `job_runs` per osservabilità; 3 test.
  - Import persone da CSV (`CORE-012`): parser RFC 4180 con separatore automatico, anteprima con errori per riga e campo, upsert per email, risoluzione manager in due passate, creazione opzionale delle unità mancanti, template scaricabile, pagina web con anteprima e conferma.
  - Form engine (`APP-001…009`): schema dichiarativo validato (10 tipi di campo, obbligatorietà, limiti, `showIf`, commento obbligatorio sotto soglia, pesi), definizioni versionate con pubblicazione e nuova versione, compilazioni con bozza/validazione/invio, punteggi per sezione e totale, risposte normalizzate per l'analytics; 7 unit test del motore e test e2e; pagine web `/forms` e `/forms/responses/[id]` con `FormRunner` generico.
- **Sprint 1 — moduli 1:1 e Feedback & Riconoscimenti** (schema, migrazioni con RLS, API, test e2e, pagine web, seed).
  - 1:1 (`ONE`): relazioni manager–riporto/mentoring/skip-level/pari, incontri con cadenza, agenda condivisa con punti riportati automaticamente al successivo, note condivise e **note private cifrate** (AES-256-GCM, chiave per tenant via HKDF da `NOTES_MASTER_KEY`), action item, suggerimenti di agenda da obiettivi a rischio, azioni scadute e feedback recenti, metriche di adozione solo aggregate per HR/manager.
  - Feedback (`FBK`): valori aziendali, feedback privato o condiviso con il manager con controllo del destinatario ("condividi con il manager", "metti in fascicolo"), presa visione e utilità, richieste di feedback con inbox e rifiuto motivato, riconoscimenti pubblici con valori, reazioni, feed per azienda/unità/team, statistiche per valore, moderazione HR.
  - Web: pagine `/one-on-ones` (elenco e creazione), `/one-on-ones/[id]` (agenda, suggerimenti, note, azioni, storico), `/feedback` (feed, ricevuti, dati, richieste, form).
- **Codice applicativo (sprint 0 della Fase 1)**: monorepo pnpm + Turborepo.
  - `packages/shared`: ruoli e permessi, claim JWT, formule di progresso OKR (unit test).
  - `packages/db`: schema Drizzle (tenant, persone, utenti, unità, storico, ruoli, naming, audit; cicli, obiettivi, key result, check-in, contributori), migrazioni SQL con Row-Level Security e ruolo `wb_app`, helper `withTenant`, runner migrazioni, database PGlite per i test, seed demo "Acme".
  - `apps/api`: NestJS su Fastify; contesto richiesta con AsyncLocalStorage; auth JWT (dev HS256 / OIDC JWKS) con login di sviluppo; guard permessi; transazione tenant per richiesta; audit log transazionale; errori RFC 9457; OpenAPI su `/docs`; moduli Core (me, tenant, persone con storico e paginazione cursor, unità con path materializzato, utenti e ruoli) e Obiettivi (cicli, obiettivi con visibilità, allineamento anti-ciclo, KR, check-in con roll-up del progresso ai padri, chiusura, albero); 19 test e2e.
  - `apps/web`: Next.js App Router con login dev, dashboard di ruolo, obiettivi (viste miei/team/albero/tutti, check-in dal browser), persone con organigramma.
  - `docker-compose.yml` (Postgres, Redis, Keycloak), `.env.example`, workflow CI GitHub Actions.
- `docs/11-guida-sviluppo.md` e screenshot dell'app reale in `docs/screenshots/`.
- Struttura iniziale del repository e documentazione di progetto.
- `README.md`, `CLAUDE.md`, `CONTRIBUTING.md`, template issue e PR.
- Visione e obiettivi (`docs/00`), benchmark di PeopleGoal usato come specifica di partenza (`docs/01`).
- Indice delle specifiche funzionali e principi trasversali (`docs/02`) con specifiche di dettaglio per modulo in `docs/specifiche/`: Core & Amministrazione, Obiettivi & OKR, Performance Review, Feedback 360°, 1:1 & Check-in, Feedback continuo & Riconoscimenti, Engagement & Survey, Sviluppo & Carriera, Onboarding, App Studio, Analytics & Reporting, Integrazioni & Notifiche.
- Architettura proposta (`docs/03`), modello dati (`docs/04`), linee guida API (`docs/05`), sicurezza e compliance (`docs/06`), UX e design system (`docs/07`), roadmap (`docs/08`), glossario (`docs/09`).
- Documento per raccogliere le nostre modifiche e differenziazioni rispetto a PeopleGoal (`docs/10`).
- ADR iniziali: adozione delle ADR (0001), stack tecnologico proposto (0002), strategia multi-tenant (0003).
- Modulo **Welfare aziendale** (`docs/specifiche/welfare.md`, codice `WEL`): piani, fonti di budget, conto welfare, categorie e soglie fiscali per anno, catalogo interno e provider, richieste e rimborsi con giustificativi, conversione premio di risultato, flussi payroll, iniziative di benessere.
- ADR-0004 architettura della reportistica (data mart storicizzato, semantic layer, query engine, connettore BI).

- Mockup HTML/PNG di 12 schermate chiave in `docs/mockups/` con script di generazione e rendering.
- ADR-0005 strategia API (REST unica per web, mobile e connettori; PKCE; token con scope; webhook firmati).
- ADR 0002 (stack), 0003 (multi-tenancy) e 0004 (reportistica) passano a **Accettato** dopo validazione con il product owner.

### Changed
- CI: rimossa la versione esplicita di pnpm nell'action (letta da `packageManager`); azioni aggiornate a checkout v5 / setup-node v5.
- `docs/specifiche/analytics.md` riscritta come motore di reportistica ingegnerizzato: catalogo metriche, report builder, report programmati, Analytics API, connettore BI, governance (ANA-040…093). Report builder e BI passano da P2 a P1.
- `docs/specifiche/app-studio.md`: low-code a livelli L1–L5; entità custom (APP-036) e automazioni (APP-037/038) confermate a P2, script sandbox e marketplace a P3.
- Propagazione in README, `docs/01` (nuova sezione "funzionalità non presenti in PeopleGoal"), `docs/02` (nuovi ruoli Analista e Welfare, principio "ogni dato è una metrica"), `docs/03` (welfare, metadata-driven, reporting separato, API unica, mobile), `docs/04`, `docs/05`, `docs/08`, `docs/09`, `docs/10` (A15, A17, A18; C2 e C4 superate).
